#ifndef HEADER
#define HEADER
#include <vector>
#endif

typedef uint32_t TYPE;

class Imem
{
public:
    vector<TYPE> imem;
    // Constructor
    Imem(const int &imem_addr_base, const int &imem_addr_offset)
    {
    }

private:
};